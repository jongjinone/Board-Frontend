import React from "react";
import {Login} from 'domain/user';

const MainPresenter = () => {

    return(
        <>
            <Login/> {/* 메인페이지는 로그인페이지로 시작함 */} 
        </>
    );
};

export default MainPresenter;